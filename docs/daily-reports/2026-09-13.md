# Daily Report — 2026-09-13

# Daily QA/PO Report — 2026-09-13

## 1. User Stories Delivered
The QA report's `user_stories` field came back empty, but cross-checking git history confirms real delivery today:
- **Issue #222 — Supporting acts / lineup on concert detail page**: shipped end-to-end via #226 (UI running order), #227 (LineupEntry model + migration), #228 (`GET /api/v1/concerts/{concert_id}/lineup` endpoint). Backend, model, and UI are all in.
- **Issue #219 — Almost-sold-out detection**: closed earlier today via #220 (threshold logic) and #221 (test coverage).
- **Issue #209/#214 — City filter**: API param and e2e coverage merged, but still **backend-only** — no picker UI in templates (tracked, not closed).

Action: the QA report generator should populate `user_stories` from merged PRs/closed issues — an empty list here undersells a 3-PR day.

## 2. Metrics
| Metric | Result |
|---|---|
| Unit tests | 123/123 passed |
| E2E tests | 17/17 passed |
| Total tests | 140/140 passed |
| Coverage | 0.0% (gate fails, threshold 70%) |
| Security (semgrep) | not run |
| Issues closed today | 111 |
| Issues open | 63 |

All functional tests are green, but coverage instrumentation is clearly broken (0.0% with 140 passing tests is not a real number) and semgrep didn't execute — neither reflects actual code quality.

## 3. Quality Assessment: **Yellow**
Functional gates (unit + e2e) are solid green. Overall status is yellow because two of four gates are non-functional rather than genuinely failing:
- Coverage: **fail** (tooling issue, not code issue — needs investigation, not a real 0%)
- Security: **not_run** (semgrep didn't execute — unverified, treat as unknown, not pass)

## 4. Risks & Blockers
- **Coverage tooling is broken.** A 0.0% reading with 140 tests passing means the report is not trustworthy; blocks any real quality signal until fixed.
- **Security scanning didn't run.** No semgrep results today — can't rule out newly introduced high-severity issues in the lineup/threshold PRs.
- **Untracked `tests/e2e/` directory** (`test_api_e2e.py` + `__pycache__`) is sitting in the working tree uncommitted — worth confirming it's intentional and either committing or cleaning up `__pycache__` before it leaks into a PR.
- **City filter UI gap persists** (#215/#218/#205): backend and badge/filter logic are done but templates still lack the picker/badge markup — three tickets are effectively blocked on the same UI work.

## 5. Tomorrow's Focus
1. Fix coverage measurement (likely a config/instrumentation issue) so the 70% gate is actually meaningful.
2. Get semgrep running again in the pipeline before merging more feature PRs.
3. Pick up the UI-only remaining work: add the city filter picker and sold-out/almost-sold-out badge markup to templates (#215, #218, #205) — backend for all three is already shipped, this is pure frontend to close out a cluster of tickets.
4. Clean up or commit the untracked `tests/e2e/` directory.
