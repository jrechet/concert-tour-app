# Daily Report — 2026-09-13

# Daily QA Report — 2026-09-13

## 1. User Stories Delivered

The demo report lists **zero user-facing stories** shipped today (`user_stories: []`). Merged work was infrastructure/plumbing for a not-yet-visible feature:

- **#220** — Added sold-out threshold logic (`is_almost_sold_out`) to the `Concert` model + config
- **#221** (closes #219) — Added test coverage for almost-sold-out detection, and along the way exposed `is_almost_sold_out` on the `ConcertResponse` schema/API (closing the #217 gap too)

No end-user can see or use this yet — the concert card template (`_concert_card.html`) only renders the existing `sold_out` badge; nothing consumes `is_almost_sold_out`. **Issue #218 (the badge itself) is the only remaining piece of the #215 "warn fan when almost sold out" epic, and it hasn't been started.**

## 2. Metrics

| Metric | Result |
|---|---|
| Unit tests | 102 / 102 passed |
| E2E tests | 15 / 15 passed |
| Total tests | 117 / 117 passed |
| Coverage | **0.0%** (reported — see risk below) |
| Open issues | 61 |
| Closed today | 108 |

All executed tests are green, but the 0% coverage figure and the 61-vs-108 open/closed mismatch both need sanity-checking (see Risks).

## 3. Quality Assessment: **Yellow**

- ✅ Unit tests: pass (102/102)
- ✅ E2E tests: pass (15/15)
- ❌ Coverage: fail (0.0% vs 70% threshold)
- ⚠️ Security (semgrep): **not run** — gate has no signal, can't call it a pass
- ⚠️ No user-facing story shipped despite a full day of merges

Test suites are healthy, but two of four gates provide no real assurance today, and the sprint produced backend plumbing with no visible increment. Not red — nothing is broken — but not a clean green either.

## 4. Risks & Blockers

- **Coverage reporting is broken, not just low.** 0.0% coverage with 117 passing tests strongly suggests the coverage tool isn't instrumenting the run rather than the code being genuinely untested. Needs investigation before trusting the gate either way.
- **Security scan didn't run.** No semgrep results this cycle — can't confirm no high-severity issues were introduced.
- **Uncommitted e2e work in the tree.** `tests/e2e/test_api_e2e.py` (173 lines) is untracked — not part of any merged PR. Confirm intent before it's lost or duplicated.
- **#218 (almost-sold-out badge) is now unblocked but unscheduled.** Model and API work landed today; the UI piece is the only thing standing between this epic and an actual demoable feature.
- **Open/closed issue counts (61 open, 108 closed today) look inconsistent** for a single day — worth confirming this isn't a reporting artifact before using it to judge velocity.

## 5. Tomorrow's Focus

1. **Ship #218** — add the almost-sold-out badge to `_concert_card.html` next to the existing `sold_out` conditional; this is the first actual user-visible outcome of the #215 epic and prereqs are now satisfied.
2. **Fix coverage instrumentation** — a 0.0% reading on a 117-test green suite is a tooling bug, not a true signal; don't let it mask real coverage gaps or false-pass future demos.
3. **Run the security gate** — get semgrep executing again so "security: not_run" doesn't stand in for "pass" in the demo report.
4. **Reconcile `tests/e2e/`** — decide whether the untracked e2e file should be committed/PR'd or was leftover scratch work.
5. Continue prioritizing tickets that produce a **visible increment** — today's merges were valuable but invisible to a demo audience two days running.
