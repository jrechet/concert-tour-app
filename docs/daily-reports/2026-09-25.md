# Daily Report — 2026-09-25

PR #369 checks out — `get_upcoming_concerts` is implemented in `src/services/concerts_service.py` and `src/routers/concerts.py`, backed by tests, so the merge is legitimate.

# Daily QA Report — 2026-09-25

## 1. User Stories Delivered
- **#366 — Add `get_upcoming_concerts` query/service method** ([PR #369](https://github.com/jrechet/concert-tour-app/pull/369), merged). Verified: service method, router wiring, and dedicated unit test file are present in the diff.
- **#367, #368** — already satisfied on `main`, no new work required.

## 2. Metrics
| Metric | Result |
|---|---|
| Unit tests | 294 / 294 passed |
| E2E tests | 26 / 26 passed |
| Total tests | 320 / 320 passed |
| Coverage | not measured (`pytest-cov` not installed) |
| Semgrep (high severity) | 0 findings |
| Issues closed today | 206 |
| Issues still open | 66 |

## 3. Quality Assessment: 🟢 **Green**
All unit and E2E suites pass with zero failures, and no high-severity security findings. The one deliverable PR was verified against its actual diff and matches its stated scope. Overall demo status: green.

## 4. Risks & Blockers
- **Coverage gate is not actually running** (`pytest-cov` missing) — the 0.0% figure is a placeholder, not a real signal. We're currently blind to coverage regressions; should be treated as a gap, not a pass.
- **66 open issues** remain unaddressed — worth a quick triage to ensure nothing stale or high-priority is being missed.
- No demo launch errors and no failing tests reported, so no immediate blockers to today's release.

## 5. Tomorrow's Focus
1. Install `pytest-cov` and restore the coverage quality gate (target 70%) — this is currently a silent gap.
2. Triage the backlog of 66 open issues to identify anything blocking or high-value for the next cycle.
3. Continue verifying PR diffs against their stated scope before merge, given repeated past incidents of mismatched PRs — keep this check in the review loop.
