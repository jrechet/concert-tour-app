# Daily Report — 2026-09-24

# Daily QA Report — 2026-09-24

## 1. User Stories Delivered
- **Days-until-concert computed field** (issue #309): backend calculation implemented and merged
  - #314 — Unit tests for `days_until_concert` calculation
  - #313 — Integration/template tests for concert card countdown display
- No other user stories reported as delivered today.

## 2. Metrics
| Metric | Value |
|---|---|
| Unit tests | 227/227 passed (0 failed) |
| E2E tests | 27 total, **0 passed / 0 failed** (did not execute) |
| Total tests | 254 |
| Coverage | 0.0% (not measured — pytest-cov not installed) |
| Issues closed today | 160 |
| Issues open | 74 |

## 3. Quality Assessment: 🟡 Yellow

- **Unit tests:** ✅ Pass — full green suite (227/227)
- **E2E tests:** ❌ Fail — 27 tests defined but none ran (0 passed + 0 failed ≠ 27 total, indicating a collection/execution failure rather than test failures)
- **Security (semgrep):** ⚪ Not run
- **Coverage:** ⚪ Not run — tooling gap (missing `pytest-cov`)

Overall status is capped at yellow because the E2E gate is broken, not just unreported — this masks whether the new countdown feature actually works end-to-end in the browser.

## 4. Risks & Blockers
- **E2E suite not executing**: `tests/e2e/test_api_e2e.py` is present but untracked in git (`?? tests/e2e/`) and the run produced 0 passed/0 failed against 27 expected — the tests likely aren't being collected/run by CI or the demo harness (possibly missing from a pytest config path, not committed, or a fixture/server startup issue). This needs investigation before we can trust the E2E gate.
- **No coverage measurement**: `pytest-cov` isn't installed, so we have no visibility into code coverage — should be added to the toolchain.
- **Security scanning not run**: semgrep gate shows `not_run` — no visibility into new high-severity findings.
- **Uncommitted E2E tests**: the e2e test file exists locally but isn't committed, so it may not exist in the branch history other reviewers/CI see.

## 5. Tomorrow's Focus
1. Fix E2E test execution (commit `tests/e2e/` if intentional, diagnose why 0/27 ran) and re-run to get a real pass/fail signal.
2. Install `pytest-cov` and establish a coverage baseline against the 70% threshold.
3. Run semgrep and clear the security gate.
4. Continue burning down the 74 open issues, prioritizing anything blocking the concert countdown feature from being demo-ready end-to-end.
