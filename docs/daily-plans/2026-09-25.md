# Daily Plan — 2026-09-25

```json
{
  "selected": [
    {
      "number": 289,
      "title": "Sort the tour dates chronologically with the next concert first",
      "reason": "Codebase inspection (src/routers/concerts.py, dashboard.html) shows this is already implemented via order_by(is_past, date_time) plus the 'next show' badge logic. Highest priority to verify first since other card features (days remaining, ticket counts) depend on correct ordering."
    },
    {
      "number": 303,
      "title": "Add a search box that filters the concert list by city",
      "reason": "A live city search box (#city-search-input, #city-filter-select) and backend `city` query param / `/cities` endpoint already exist in dashboard.html and concert-search.js. Needs verification against the issue's acceptance criteria and closing rather than re-implementation."
    },
    {
      "number": 231,
      "title": "Let a fan search concerts by artist name",
      "reason": "An artist-name search box wired via HTMX already exists in dashboard.html, backed by `artist_name` filtering in both the dashboard and JSON API routers. Needs verification/closure."
    }
  ],
  "daily_plan": "## Today's goal: verify and close out functionality that appears already shipped\n\nA source inspection ahead of sprint planning turned up something important: **every open backlog issue (#316/#296, #308, #303, #296, #289, #280, #259/#252, #231, #222) already appears to be implemented** in `main` — sold-out badges, remaining-ticket counts, days-until-concert, chronological/next-show sorting, the `/api/v1/concerts/upcoming` JSON endpoint, city and artist search boxes, and the full concert-detail lineup view are all present in `src/models/concert.py`, `src/routers/concerts.py`, `src/templates/`, and `src/static/js/concert-search.js`, with corresponding test files already in `tests/`. Given the team's standing rule against opening PRs that don't actually change behavior, we should not re-implement work that's already done.\n\nToday's priority is therefore **verification over new development**: run the existing test suites for #289 (ordering), #303 (city search), and #231 (artist search) end-to-end, confirm the UI matches each issue's acceptance criteria in a real browser session, and then close those issues with a comment linking the implementing commits/tests rather than opening redundant PRs. This protects velocity metrics from being inflated by no-op PRs and keeps the backlog honest.\n\nRisk: the backlog also contains two duplicate pairs — #316/#296 (sold-out badge) and #259/#252 (ticket count) — which should be closed as duplicates during the same pass to reduce noise for future sprint planning. If verification of #289, #303, or #231 surfaces a genuine gap versus the issue's acceptance criteria (e.g. an edge case not covered by the existing tests), that gap becomes the actual, narrowly-scoped implementation task for the day instead of a blanket re-build.\n\nDependency order: ordering (#289) underpins the other two since both search flows re-render the same card list and rely on correct sort/next-show behavior, so it's verified first."
}
```
