# Daily Plan — 2026-09-25

Code review of `main` shows most of the open backlog is already implemented, not just planned: `GET /api/v1/concerts/upcoming` (#280), the `/{concert_id}/lineup` endpoint plus its rendering in `concert_detail.html` (#222), the artist-name search box wired end-to-end on the dashboard (#231), the sold-out badge and remaining-ticket count rendered in both the server template and `concert-search.js` (#316, #252), the debounced city search box with dropdown (#303), and `GET /api/v1/stats/upcoming-count` with its service, schema, and router registration (#332, closed out by PR #336's tests). None of these have real gaps in the code — they should be verified against acceptance criteria and closed rather than re-worked, which avoids duplicate effort today.

The one story with a genuine, verified gap is **#337 (filter by venue)**: `GET /api/v1/stats/venues` already exists to populate a dropdown, and `get_distinct_venue_names` is in the stats service, but there is no `venue` query parameter on `GET /api/v1/concerts/` or the `/api/v1/dashboard/concerts` HTML route, and no UI control wired to it — unlike the city filter, which has both the backend param and the JS wiring. This mirrors the existing city-filter pattern closely enough (same join through `Concert.venue_id`, same case-insensitive substring match) that it's low-risk, well-scoped work for today.

**Today's plan**: implement #337 end-to-end — add a `venue` query param to both concert-listing endpoints following the exact pattern already used for `city`, then wire a venue filter control into the dashboard (input + optional dropdown backed by `/api/v1/stats/venues`) mirroring `concert-search.js`'s city-search implementation. Main risk is scope creep into re-touching the already-complete city/artist filters — keep the diff additive and isolated to the venue param and its UI hookup, and write tests analogous to `test_concert_city_filter.py`/`test_concert_city_search_ui.py`. No other backlog issue has enough unimplemented surface to justify a second story today; recommend the team spend remaining time verifying and closing #332, #280, #222, #231, #316, #252, and #303 instead of opening new PRs against them.
