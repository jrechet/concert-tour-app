# Daily Plan — 2026-09-25

Today's priority is closing out the ticket-availability feature pair: #252 (remaining ticket count on cards) is the foundational data/backend piece, and #316 (sold out badge) is a small UI layer that depends directly on that same ticket-count data being exposed on the concert model/API — sequencing them together avoids a repeat of past PR mismatches where UI claimed backend support that wasn't there. Dev should implement #252 first (model field/endpoint + card display), verify it in the diff, then layer #316's conditional badge on top of it.

In parallel, we're adding #280 (JSON endpoint listing upcoming concerts) since it's an independent, low-risk backend addition that extends read APIs similarly to the recently-shipped stats/count and venue-filtering work, and unblocks future consumers (e.g. widgets, integrations) without touching the ticket-count code path.

Risks: the team has a track record of PRs claiming test/feature additions with diffs that don't match (see #306/#307/#192/#257 in memory) — QA must diff-check that #252 actually adds a ticket-count field and that #316's badge logic reads it, not just static markup. Deferred: #344 (CSV export), #303/#231 (search boxes), #222 (line-up) are backlogged for a future cycle since they're independent of today's ticket-availability focus and lower urgency.
