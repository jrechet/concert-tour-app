# Daily Plan — 2026-09-25

Today's priority is #352 (Summarise a tour) — it's the only backlog item without existing implementation. A quick audit of `main` shows the rest of the backlog is already done: CSV export (#354/#351/#344 are duplicates of the just-merged #348-#350 work), city search with both a live input and a populated dropdown (#303, `src/templates/dashboard.html` + `src/static/js/concert-search.js` + `GET /api/v1/concerts/cities`), artist-name search on both the JSON API and the HTMX dashboard (#231), the full concert line-up rendered on `concert_detail.html` and served via `GET /api/v1/concerts/{id}/lineup` (#222), and `GET /api/v1/stats/upcoming-count` (#332) already exists in `src/routers/stats.py`.

Rather than burn a dev cycle re-implementing shipped features, today's plan is to build #352 (a tour summary — likely an endpoint/view aggregating a tour's dates, cities, and status) as new work, and separately flag #354/#351/#344/#303/#231/#222/#332 to be closed as duplicates or already-resolved once verified, so the backlog stops resurfacing completed work.

Main risk: #352 has no detailed spec (just a title), so Dev should scope it narrowly — a read-only aggregate endpoint (e.g. concert count, date range, cities visited) built on the existing Tour/Concert/Venue models — rather than guessing at a broader feature. No dependency blockers: all underlying models (Tour, Concert, Venue) already exist and are queried elsewhere (e.g. `get_tour_dates` in `src/routers/tours.py`), so this is additive, not foundational, work.
