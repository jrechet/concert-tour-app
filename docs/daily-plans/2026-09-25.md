# Daily Plan — 2026-09-25

Today's focus is closing out the in-progress stats backend and kicking off ticket-count visibility for fans. #328 and #321 extend the schema/query-logic groundwork merged yesterday in #327, so finishing both stats endpoints today avoids leaving that infrastructure half-wired and gives the frontend team read-only JSON data to build against. These are low-risk, additive endpoints with no dependency conflicts.

The third priority, #259 (remaining ticket count on concert cards), is picked deliberately over #316 (sold out badge) because the badge logically depends on ticket-count data being surfaced first — implementing #259 today sets up #316 as a clean, low-effort follow-up next sprint rather than doing both half-baked at once.

Note: #252 is a duplicate of #259 (identical title/scope). Recommend closing #252 as a duplicate once #259 is merged, to avoid two agents implementing the same card change in parallel.

Main risk is endpoint/route collisions between #328 and #321 if both touch the same stats router file — sequence the PRs (count first, then cities) and require a rebase before merging the second. No infra or dependency changes are planned today, consistent with prior guidance to scope backend work narrowly.
