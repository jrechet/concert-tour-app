# Daily Plan — 2026-09-25

I audited the codebase before planning and found that #316, #303, #280, #252, #231, and #222 already have complete backend/frontend implementations and passing tests (e.g. `GET /api/v1/concerts/upcoming`, `artist_name`/`city` filters, sold-out badges, remaining-ticket counts, lineup rendering all exist in `src/`). Only #332 represents genuinely unimplemented work.

```json
{
  "selected": [
    {"number": 332, "title": "Add GET /api/v1/stats/upcoming-count returning how many concerts are today or later", "reason": "The only backlog item with no existing implementation — src/routers/stats.py currently only exposes /cities and /venues, and stats_service.py has no count logic. Straightforward addition that reuses the same today-or-later date-comparison pattern already established in GET /api/v1/concerts/upcoming (routers/concerts.py), so dependency-wise it's low risk and quick to land."}
  ],
  "daily_plan": "Codebase audit before planning showed the backlog is mostly stale: #316 (sold-out badge), #303 (city search box), #280 (upcoming JSON endpoint), #252 (remaining ticket count), #231 (artist search), and #222 (lineup display) are all already implemented end-to-end — models expose `remaining_tickets`/`sold_out`, the concerts router supports `city`/`artist_name`/`upcoming_only` filters and a dedicated `/upcoming` endpoint, templates render ticket badges and lineups, and each has dedicated passing tests (test_concert_cards.py, test_concert_city_search_ui.py, test_concerts_upcoming.py, test_concert_search.py, test_concert_lineup_api.py, etc.). Assigning dev work against these would risk duplicate/no-op PRs, which past cycles have flagged as a failure mode.\n\nToday's only real development goal is #332: add `GET /api/v1/stats/upcoming-count` to `src/routers/stats.py`, backed by a `get_upcoming_count` function in `stats_service.py`, using the same calendar-date-vs-`reference_time` comparison already used by the `/concerts/upcoming` endpoint for consistency. This is small, low-dependency, and should ship with a unit test and an API test in a single PR.\n\nSecondary priority for today: verify the six already-implemented issues against their acceptance criteria and close them with a note referencing the existing tests/PRs, so the backlog reflects reality and future cycles don't re-attempt completed work. Risk is low since no new backend changes are involved in that cleanup — it's read-only verification, not code changes."
}
```
