# Daily Plan — 2026-09-25

Today's focus is knocking out one high-value backend/export feature and starting fan-facing discovery features, while cleaning up duplicate backlog noise. Three open issues (#344, #351, #354) all request the same "Export concerts as CSV" feature — we're taking only #344 (the earliest-filed) into today's sprint and recommend closing #351 and #354 as duplicates once #344 ships, to avoid wasted parallel effort or conflicting PRs.

Priority 1 is #344 (CSV export) since it's a self-contained, high-value feature for organizers/fans wanting offline data, with no dependencies on other backlog items. Priority 2 is #222 (full concert line-up view), which is foundational for fans and likely shares model/detail-endpoint work that #303's search feature can build on later. Priority 3 is #303 (city search box), a straightforward frontend filter that adds immediate UX value once the concert list/detail data model is solid.

Risks: the CSV export must serialize real concert data end-to-end (model → endpoint → CSV writer), not just add tests or a stub — per prior retrospectives, PRs that don't match their described functionality should be rejected. For #222, ensure line-up/artist relationships exist in the data model before building the endpoint; if line-up data isn't modeled yet, this could balloon into schema work. For #303, coordinate with #231 (artist search) since both are list-filtering features — build the city filter in a way that doesn't preclude adding artist-name filtering next.
