# Daily Plan — 2026-09-25

## Today's Goals
The backlog is almost entirely stale: a code audit confirms CSV export (`GET /api/v1/concerts/export.csv`, `src/services/concerts_service.py`), city search (`GET /api/v1/concerts/cities` + `concert-search.js` + dashboard dropdown), artist-name search (`artist_name` query param on both the JSON API and HTMX dashboard route), the full concert line-up (`GET /api/v1/concerts/{id}/lineup`, rendered on `concert_detail.html`), and `GET /api/v1/stats/upcoming-count` are all already shipped on `main`. Issues #354, #351, #344, #303, #231, #222, and #332 describe functionality that already exists and should be closed as duplicates/already-resolved rather than re-implemented.

## Priority
The only genuinely new item is #358 ("Summarise a tour"), which has no existing implementation anywhere in the codebase. That's today's sole development focus: a read-only aggregate endpoint/view built on the existing Tour/Concert/Venue models (e.g. date range, number of concerts, cities visited, cancellation status), following the pattern already established by `get_tour_dates` in `src/routers/tours.py`.

## Risks
#358 has no detailed spec — just a title — so Dev must scope narrowly (a simple aggregate endpoint) rather than guessing at a broader feature, and should add tests + pagination-appropriate handling consistent with existing endpoints. No dependency blockers: Tour, Concert, and Venue models already exist and are queried elsewhere, so this is additive work. Secondary risk: re-flagging the duplicate issues (#354/#351/#344/#303/#231/#222/#332) for closure so they stop resurfacing in future planning cycles and wasting dev time re-verifying already-shipped features.
