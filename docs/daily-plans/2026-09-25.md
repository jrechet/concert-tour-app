# Daily Plan — 2026-09-25

## Goals for 2026-09-25
Today's sprint focuses on shipping visible, low-risk fan-facing improvements to the concert list. #296 (sold out badge) is ready to build immediately since the backing `is_sold_out` field just landed in #320 — this is the top priority and should be dependency-free. In parallel, #289 (chronological sort) fixes a foundational ordering bug and sets up clean groundwork for #308 (days-remaining countdown), which should follow directly after.

**Priorities:** 1) #296 sold-out badge, 2) #289 chronological sort, 3) #308 days-remaining. Sequencing #289 before #308 avoids rework, since days-remaining logic is more reliable once dates are consistently ordered.

**Risks:** #296 and #316 are duplicate issues with identical titles — team should implement against #296 and close #316 as a duplicate afterward to avoid wasted double-work. Similarly, #259/#252 (remaining ticket count) are duplicates and #303/#231 (city vs artist search) are related search features; none were selected today but should be deduped and prioritized together in the next sprint. Given recent memory notes on PRs claiming unimplemented functionality, each PR today must be verified to contain real schema/frontend changes matching its description before merge.
