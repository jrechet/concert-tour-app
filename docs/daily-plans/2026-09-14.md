# Daily Plan — 2026-09-14

Confirmed #231 (search by artist) has no existing implementation — genuinely new work.

```json
{"selected":[{"number":231,"title":"Let a fan search concerts by artist name","reason":"No existing search functionality on main; highest net-new business value (helps fans find concerts directly) and unblocked by any prior work"},{"number":215,"title":"Warn a fan when a concert is almost sold out","reason":"Backend (is_almost_sold_out) and API already shipped via #220/#221; only the badge markup in _concert_card.html is missing, so this is a fast, low-risk win"},{"number":205,"title":"Let a fan filter the concert list by city","reason":"API and test coverage already shipped (#214); dashboard.html has no city picker UI yet, so this closes out an already-invested feature"}],"daily_plan":"Today's sprint focuses on closing out two nearly-finished features and kicking off one net-new capability. #215 and #205 both have their backend/API work already merged from prior sprints — the remaining work is purely template/UI: add an 'almost sold out' badge variant to `_concert_card.html` (distinct from the existing plain 'Sold Out' badge) and add a city filter control to `dashboard.html` wired to the existing filtered endpoint. These are low-risk, high-leverage since the hard part is done.\n\n#231 (search by artist name) is genuinely new — no search UI or backend query exists yet. This should follow the established pattern: model/query layer and API endpoint first, then template/frontend, then tests, per our standing rule to implement foundational layers before dependent components. Treat this as the primary engineering push of the day; the other two tickets are cleanup that a single dev-agent can finish quickly in parallel.\n\nMain risk: agents re-implementing backend logic that already exists for #215/#205 instead of just adding UI — worth flagging explicitly in ticket instructions to avoid duplicate work. #222 and #198 are confirmed fully shipped (lineup UI and ticket-count card both verified in templates) and should not be reselected."}
```
