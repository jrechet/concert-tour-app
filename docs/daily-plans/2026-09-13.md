# Daily Plan — 2026-09-13

```json
{
  "selected": [
    {
      "number": 198,
      "title": "Show the remaining ticket count on each concert card",
      "reason": "Only open backlog item; directly extends the ticket-availability work already shipped in #201/#203/#204 (remaining_tickets and sold_out now exposed via the concerts API), so the dependency is already satisfied and this closes out the feature end-to-end on the frontend."
    }
  ],
  "daily_plan": "Today's primary goal is to finish the remaining-ticket-count feature by wiring #198 into the UI. The backend groundwork landed yesterday (#204 exposed `remaining_tickets` and `sold_out` on the concerts API, and #203 added the sold-out badge to the concert card), so this should be a relatively contained frontend task: render the numeric remaining-ticket count alongside the existing sold-out badge on each concert card, pulling from the fields already returned by the API.\n\nSince the backlog only has this single open issue, today's sprint is deliberately small — one focused ticket rather than parallel workstreams. Priority is to ship #198 cleanly and verify it against the live API response (including edge cases like `remaining_tickets: 0`, which should defer to the sold-out badge rather than showing '0 tickets left' redundantly).\n\nMain risk is scope creep or over-engineering a one-field UI change — per team convention, this should stay a narrow, well-tested addition without introducing new abstractions or touching unrelated card layout code. Confirm the PR's diff actually matches the described change before merging, consistent with past review findings on this project.\n\nAfter #198 ships, the backlog will be empty. Flag to stakeholders that new user stories are needed to keep the sprint pipeline full — avoid pulling in bot-generated tracking issues (e.g. Renovate) as substitute sprint work."
}
```
