# Daily Plan — 2026-09-13

```json
{
  "selected": [
    {"number": 215, "title": "Warn a fan when a concert is almost sold out", "reason": "Backend prereqs (Concert.is_almost_sold_out, ConcertResponse.is_almost_sold_out) already shipped via #220/#221; only the badge markup in _concert_card.html is missing. Low risk, high fan-urgency value, ready to implement today."},
    {"number": 205, "title": "Let a fan filter the concert list by city", "reason": "City filter API (?city=) and tests already shipped; only a city picker UI in the dashboard template is missing. Quick win that unlocks a real UX gap for fans browsing many concerts."},
    {"number": 222, "title": "Let a fan see the full line-up for a concert", "reason": "No per-concert lineup/performer data model exists yet (Tour only has a single artist field) — this is net-new scope, so we start the model/API groundwork today per the team's foundational-layers-first rule, even though it won't ship UI in one day."}
  ],
  "daily_plan": "Today's sprint focuses on closing out two nearly-finished features before starting a larger net-new one. Verification against the current codebase confirmed #198 (ticket count) is already fully shipped and should stay out of scope, while #215 and #205 both have their backend/API work done and are blocked only on template/UI changes — these are fast, low-risk wins that should ship today.\n\nPriority order: #215 first (add an `is_almost_sold_out` badge conditional in `_concert_card.html` next to the existing `sold_out` check), then #205 (add a city `<select>`/query-param picker to `dashboard_concerts.html` wired to the existing `?city=` filter). Both are additive template changes with no model/migration risk.\n\n#222 is a different shape of work: there is currently no data model for a concert's line-up or support acts (Tour only stores a single `artist` field), so this needs proper sequencing — model/migration first, then schema/API exposure, then template rendering — rather than being treated as a one-PR UI task. We'll kick off the model design and migration today, but it will likely span into a follow-up session before the UI is user-visible.\n\nMain risk: don't let #222 get rushed into a template change before its backing model exists, and don't accidentally re-touch `_concert_card.html`'s sold-out logic in a way that regresses the #219/#220/#221 work already merged."
}
```
