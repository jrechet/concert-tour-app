# Daily Plan — 2026-09-13

Today's backlog has exactly one real user story, filed twice as duplicate issues (#197/#198, identical title 'Show the remaining ticket count on each concert card'). I've selected #197 as the canonical ticket and recommend closing #198 as a duplicate rather than tracking both — running them in parallel would just produce conflicting PRs against the same code.

I verified current main state before selecting anything, per the standing lesson that this project's dashboard epic has repeatedly been scheduled ahead of its prerequisites. `src/models/concert.py` and `src/models/venue.py` exist now (landed via PR #196), and `Venue.capacity` is populated, but there is no `tickets_sold`/`tickets_available` field anywhere in the schema — so 'remaining tickets' has no data to compute from. Separately, `dashboard.html`'s `#calendar-grid` section is still a literal empty shell with a comment saying it's 'populated by a later task' — there is no concert-card component yet to attach a ticket count to. There's also no Concert router (`src/routers/` only has `tours.py`), so nothing currently serves per-concert data to the dashboard at all.

Given that, today's priority is sequencing this correctly instead of repeating the pattern where a narrowly-scoped ticket gets 'closed' against markup/data that don't exist. Plan: (1) add a `tickets_sold` (or `tickets_remaining`) column to the `Concert` model plus a minimal Concert router/endpoint so ticket data is actually queryable; (2) build the concert-card partial inside `#calendar-grid` (currently just a shell) that the dashboard already wires up via htmx; (3) render the remaining-ticket count on that card using capacity minus sold. If time only allows part of this, land (1) and (2) today and treat the actual count display as a fast follow — better to under-promise than to report the issue closed with disabled/placeholder markup again.

Main risk: this is the same dashboard epic that has stalled and been re-derived across a dozen abandoned branches (issues #97/98/99/146/148/155-162, #185-190) — several "feat" commits on those branches turned out to contain zero real code. I'll verify actual diff contents (not just PR titles) before reporting any part of this done, and will flag explicitly in the PR if only the data-model/card-shell prerequisites land today rather than the full ticket-count feature.
