# Daily Plan — 2026-09-13

Now returning the sprint selection based on verified repo state.

```json
{
  "selected": [
    {"number": 205, "title": "Let a fan filter the concert list by city", "reason": "Backend API (?city= param) and tests already shipped in PRs #209/#214, but no fan-facing UI exists anywhere in src/templates — no city picker/dropdown on the concerts page despite the 'end-to-end' test label (those tests only hit the API layer). This is the highest-value remaining gap: a small, low-risk frontend-only PR that finally makes the feature usable by fans, unblocked by any missing backend work."},
    {"number": 215, "title": "Warn a fan when a concert is almost sold out", "reason": "Net-new feature with no blockers — remaining_tickets and sold_out are already computed and exposed via the API and rendered on the concert card (src/models/concert.py, _concert_card.html), so adding an 'almost sold out' threshold warning is a natural, low-risk extension of existing, tested infrastructure."}
  ],
  "daily_plan": "Today's sprint focuses on closing the real user-facing gap in the city filter feature and adding urgency messaging for near-sold-out concerts. Verification of the repo turned up that issue #198 (ticket count on concert cards) is already fully shipped on main — badge and remaining-ticket text render correctly in `_concert_card.html`, backed by tested API fields — so it's excluded from today's plan and should be closed rather than re-scheduled.\n\nIssue #205 looked complete from commit history (PRs #209 and #214 mention 'end-to-end test coverage'), but inspection showed those tests only exercise the FastAPI TestClient against `/api/v1/concerts/?city=` — there is no actual city picker or filter control in any template. This is today's priority: build the missing UI (a select/input wired to the existing, already-tested `?city=` param) so fans can actually use the feature the issue describes. Because the backend is solid, this should stay scoped to templates/frontend wiring only — no new API or model work needed.\n\nIssue #215 (almost-sold-out warning) is independent, net-new work that builds directly on the `remaining_tickets`/`sold_out` fields already computed in `src/models/concert.py` and exposed via the API. It can proceed in parallel with the #205 UI work since neither depends on the other.\n\nMain risk today is scope creep on #205 — since the backend already exists, the team should resist the temptation to touch `src/routers/concerts.py` or add new API tests, and instead focus purely on the missing frontend control. For #215, the main risk is picking an arbitrary 'almost sold out' threshold without a stated business rule; default to a sensible fixed threshold (e.g. <10% or <50 tickets remaining) and flag it for PO review rather than blocking on it."
}
```
