# Daily Plan — 2026-09-13

Confirmed no search-by-artist endpoint exists yet — `#229` needs full backend + frontend work, similar to how `#205`'s city filter query param was built. Here's today's sprint selection.

```json
{
  "selected": [
    {"number": 215, "title": "Warn a fan when a concert is almost sold out", "reason": "Backend fully shipped (Concert.is_almost_sold_out landed via #220/#221 and is exposed in the API schema) — only the card template needs an 'almost sold out' badge alongside the existing 'Sold Out' state. Lowest risk, highest-value quick win: surfaces urgency that drives ticket conversions."},
    {"number": 205, "title": "Let a fan filter the concert list by city", "reason": "City filter query param, join logic, and tests already shipped on the API; the dashboard template has no picker UI wired to it. Same shape as #215 — finish the loop on already-invested backend work before starting new features."},
    {"number": 229, "title": "Let a fan search concerts by artist name", "reason": "No backend support exists yet (Tour.artist is a plain column with no search endpoint). Net-new but high business value for fan discovery; sequencing it after the two UI-completion tickets lets today's team start it with full model→API→UI runway rather than rushing it in alongside #222-style foundational work."}
  ],
  "daily_plan": "Today's sprint closes out two nearly-finished features before starting one net-new one. #215 and #205 both had their backend work (sold-out threshold logic, city filter query param) completed in yesterday's sprint but never got template/UI treatment — picking them up first converts already-sunk backend investment into shipped fan-facing value with minimal risk, since no new models or migrations are needed, just wiring existing API fields into `_concert_card.html` and `dashboard.html`.\n\n#229 (search by artist name) is the stretch goal: verification shows there's no backend support today (`Tour.artist` is a plain string column with no query endpoint), so this needs the full model→API→UI sequence the team follows for foundational work — a new search endpoint (likely a `q`/`artist` query param on the concerts list, mirroring the #205 city-filter pattern) plus a search box in the dashboard template. If time runs short, the endpoint and tests should land today even if the UI slips to tomorrow.\n\nRisk notes: don't let #215/#205 UI work silently diverge from the sold_out/remaining_tickets and city-filter contracts already defined in the API — reuse those fields rather than re-deriving state in templates. For #229, watch for scope creep into fuzzy/partial-match search; a case-insensitive substring match consistent with the existing city filter's case-insensitive exact match is sufficient for v1. Also confirmed today that #222 (concert lineup) and #198 (ticket count) are both fully shipped and should not be re-selected in future planning."
}
```
