# Daily Plan — 2026-09-13

```json
{
  "selected": [
    {
      "number": 205,
      "title": "Let a fan filter the concert list by city",
      "reason": "Backend support already exists (city query param on GET /api/v1/concerts/, added by PR #209 closing #206), but the dashboard's HTMX concert-list endpoint (GET /api/v1/dashboard/concerts) and dashboard.html have no city filter input or param wiring at all. This is the highest-value, lowest-risk item: finish the already-started feature end-to-end rather than starting something new."
    }
  ],
  "daily_plan": "Today's sprint has exactly one genuinely open story: #205, letting a fan filter the concert list by city. The consumer-facing JSON API (`/api/v1/concerts/`) already supports a `city` query param from the recent #209 work, but the dashboard's HTMX fragment endpoint (`get_dashboard_concerts` in `src/routers/concerts.py`) ignores it entirely, and there is no filter input anywhere in `dashboard.html` or `dashboard_concerts.html`. Today's work is to add a `city` query param to the dashboard endpoint (reusing the same case-insensitive join/filter pattern already proven in the JSON API), add a simple city filter control to `dashboard.html` wired via `hx-get`/`hx-trigger` to `dashboard_concerts.html`, and cover it with an integration test.\n\nI'm not scheduling #198 (remaining ticket count on concert cards) as dev work this sprint. Checking the current repo state, it's already fully shipped: `_concert_card.html` renders the sold-out badge and \"N tickets left\" text, backed by `remaining_tickets`/`sold_out` fields exposed via prior PRs (#203/#204), with schema-level tests already in place. Scheduling it again would duplicate finished work — recommend closing #198 on GitHub, or downgrading it to a quick verification/integration-test pass if the team wants extra coverage, rather than treating it as a feature build.\n\nMain risk for #205 is scope creep: the JSON API's city filter is case-insensitive exact match on venue city, and the dashboard filter should mirror that behavior exactly rather than introducing fuzzy or partial matching, to keep API and dashboard behavior consistent. Since this touches the dashboard fragment endpoint, verify no existing dashboard tests assume an unfiltered concert list before landing the change, and make sure any new template partial is included in the PR (per past team learning: referencing templates without shipping them breaks the app).\n\nGiven the backlog is otherwise empty of real feature work, next planning cycle should focus on triaging GitHub issue hygiene (closing stale/duplicate issues like #198 and the earlier #205/#206 overlap) so the backlog accurately reflects remaining work."
}
```
