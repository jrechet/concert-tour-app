# Daily Plan — 2026-09-24

Confirmed with real test coverage backing each feature.

```json
{
  "selected": [],
  "daily_plan": "## Backlog audit finding: nothing new to sprint on\n\nI reviewed every open backlog issue against the current `main` codebase before selecting work, and all twelve are already implemented and tested: chronological tour-date ordering (#289/#273) in `GET /api/v1/concerts/` and `/upcoming`; city search with a live UI filter and dropdown (#285/#266) via `GET /api/v1/concerts/cities`, `?city=` filtering, and `concert-search.js`; artist-name search (#231) via the dashboard's HTMX search box and `artist_name` filter; remaining-ticket-count and sold-out badges (#259/#252/#246/#198/#265) as `Concert.remaining_tickets`/`sold_out`/`is_almost_sold_out` properties, exposed through `ConcertResponse` and rendered in both the server-rendered card template and the JS-rendered one; the upcoming-concerts JSON endpoint (#280) at `/api/v1/concerts/upcoming`; and full concert line-up viewing (#222) via `/api/v1/concerts/{id}/lineup` and `concert_detail.html`. Each has corresponding integration tests (e.g. `test_concerts_upcoming.py`, `test_concert_search.py`, `test_concert_sold_out_threshold.py`, `test_concert_lineup_api.py`).\n\nGiven that, selecting any of these for implementation today would risk exactly the duplicate/no-op PR pattern flagged in past retros (PR #192, #257). Instead, today's priority is backlog hygiene: confirm the full test suite passes on `main`, then close these twelve issues as already-shipped (linking the relevant commits/tests as evidence) so the backlog reflects reality.\n\n**Risk:** none of the above represents real business value left to capture; the risk is wasted engineering effort or accidental regressions if someone re-implements an existing feature. **Next step:** once the backlog is cleaned up, groom for genuinely new stories — nothing in the current list qualifies for today's sprint."
}
```
