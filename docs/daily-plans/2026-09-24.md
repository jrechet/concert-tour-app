# Daily Plan — 2026-09-24

Today's sprint focuses on closing out the real gap behind the remaining-ticket-count feature before layering the sold-out badge on top of it. Agent memory flags that a previous PR (#257) falsely claimed to implement ticket-count exposure while only adding tests, so we're treating #198 as a from-scratch build: model/schema field, service logic, and the API endpoint, verified end-to-end before marking it done. This is the foundational piece for today.

Once ticket count is genuinely available on the concert model/API, #265 (sold-out badge) becomes a straightforward frontend/API consumer of that data rather than requiring its own backend plumbing — sequencing these two together avoids rework and keeps the diff honest per our verification rule.

In parallel, #266 (city search box) is independent of the ticket-count work and delivers clear fan value, so it can proceed without blocking on the other two stories. All three issues have duplicate numbers still open in the backlog (#246/#252/#259 for ticket count, #296 for sold-out badge, #285 for search) — once today's work lands and is verified against the actual diff, those duplicates should be closed as resolved rather than re-implemented.

Main risk today is repeating the #257 failure mode: any PR closing #198 or #265 must be checked to confirm the diff contains real schema/model/endpoint changes, not just tests or stubs, before merging.
