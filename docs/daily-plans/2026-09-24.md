# Daily Plan — 2026-09-24

Today's sprint focuses on three independent, high-value improvements to the concert listing experience: remaining ticket counts per card, chronological sorting of tour dates, and city-based search. We're deliberately picking the lowest-numbered issue for each duplicated request (#246, #273, #285) rather than working all near-duplicates, to avoid redundant PRs against the same code paths.

Priority order is ticket-count display first, since it establishes the ticket-availability data/service layer that a future 'sold out' badge (#296) will depend on — we're intentionally deferring that badge to next sprint to keep today's scope tight and avoid stacking a dependent feature on unfinished work. Chronological sorting and city search are independent of each other and of the ticket-count work, so they can proceed in parallel.

Main risks: duplicate open issues (#252/#259/#296 for tickets, #289 for sorting, #303/#231 for search) create a risk of overlapping or conflicting PRs if agents pick up the near-duplicate issues instead of the selected ones — dev agents should be pointed explicitly at #246, #273, and #285. As always, verify each PR's diff actually contains the claimed model/endpoint/frontend changes before merging, per past retrospective failures (PR #192, #257, #307).
