# Daily Plan — 2026-09-24

All the backend, template, and JS code I inspected shows that essentially every feature in the current backlog — sold-out badge, remaining ticket count, city search, artist search, chronological sort with "Next Show" highlight, the upcoming-concerts JSON endpoint, and full lineup display — is already implemented, tested (240/240 passing per today's QA report), and confirmed as real (non-stub) implementation. The backlog is stale/duplicated rather than reflecting actual open work.

```json
{
  "selected": [
    {"number": 296, "title": "Show a \"sold out\" badge on a concert card when no tickets remain", "reason": "Already fully implemented (Concert.sold_out/remaining_tickets model properties, ConcertResponse schema fields, _concert_card.html and concert-search.js rendering) and verified today per the QA report. Selecting it to formally verify-and-close, deduping #259/#252/#246 against it rather than re-building."},
    {"number": 303, "title": "Add a search box that filters the concert list by city", "reason": "Already fully implemented end-to-end: GET /api/v1/concerts/?city=, GET /api/v1/concerts/cities, and a wired city-search-input/city-filter-select in dashboard.html + concert-search.js. Selecting for verify-and-close, deduping against #285."},
    {"number": 289, "title": "Sort the tour dates chronologically with the next concert first", "reason": "Already implemented via order_by(is_past, date_time) plus the 'Next Show' badge in both the server-rendered template and client-side JS. Selecting for verify-and-close, deduping against #273."}
  ],
  "daily_plan": "## Today's goal: backlog hygiene, not new builds\nA code review before planning showed that essentially the entire open backlog — sold-out badges, remaining ticket counts, city search, artist search, chronological sort/Next Show highlighting, the upcoming-concerts JSON endpoint, and full concert lineups — is already implemented across model, schema, API, and both server-rendered and JS-rendered UI, and covered by the 240/240 passing test suite confirmed in today's QA report. The backlog issues listed for this sprint are duplicates or stale copies of already-delivered work, not new stories.\n\nGiven that, today's priority is verification and closure rather than development: confirm the three selected issues (#296, #303, #289) against their real diffs/tests one more time, then close them along with their duplicates (#259, #252, #246 for ticket count/sold-out; #285 for city search; #273 for chronological sort) with evidence attached, and separately close #280, #231, and #222 which are likewise already complete (upcoming JSON endpoint, artist-name search, and lineup display all exist and work).\n\nRisk: the team has a documented history (PR #192, PR #257) of claiming functionality that isn't in the diff. Today's risk runs the opposite direction — real functionality exists but issues stay open and keep getting re-filed as duplicates, wasting planning cycles. Closing verified duplicates now prevents them from being re-selected in future sprints.\n\nSeparately, today's QA report flagged that coverage tooling (pytest-cov) and the semgrep security gate aren't running at all — that's the actual open risk worth engineering attention next, once backlog cleanup is done, rather than any of the currently listed user stories."
}
```
