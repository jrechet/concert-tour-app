# Daily Plan — 2026-09-23

Today's sprint focuses on closing out two features that are backend-complete but missing their front-end surface. Repo inspection confirms #215's is_almost_sold_out model property and schema field exist and are exposed via the API, but _concert_card.html only renders a sold_out badge and a raw ticket count — no 'almost sold out' state is shown to fans. Similarly for #205, city is displayed as plain text on concert cards and the detail page, but there is no filter/dropdown control anywhere in dashboard.html, unlike the artist-name search box which already has a working hx-trigger input. Both are template-only additions with no new backend work required, making them fast, low-risk wins.

Priority order: #215 first (higher business value — urgency messaging near a sellout is a stronger conversion lever than filtering), then #205. Risk is low for both since backend logic and tests already exist; the main risk is scope creep into unrelated template refactors, so implementers should touch only the minimal markup/hx-get params needed.

Not selecting: #198, #246, and #252 are duplicate titles for 'show remaining ticket count on each concert card' — verified already shipped in _concert_card.html (renders '{{ concert.remaining_tickets }} tickets left') as of 2026-09-13. #231 (artist search) and #222 (lineup display) were also verified present in the current templates (#concert-search block in dashboard.html, .concert-lineup-list in concert_detail.html) and should not be reselected. Recommend closing #246/#252 as duplicates of the already-shipped #198 rather than carrying them forward in the backlog.

Stretch goal if both land early: check whether #218, which duplicates the almost-sold-out badge work, should be closed once #215 merges.
