# Daily Plan — 2026-09-23

Memory tools aren't available in this session (read-only context), so I'll skip persisting notes and just return the sprint selection based on verified code state.

```json
{
  "selected": [],
  "daily_plan": "## Today's Goals\n\nAfter verifying the codebase directly, every issue currently open in the backlog is already fully implemented on `main`. Issues #259, #252, #246, and #198 all share the identical title \"Show the remaining ticket count on each concert card\" and are duplicates of previously-shipped work — the ticket count badge and sold-out state are already rendered in `src/templates/_concert_card.html` (lines 12-16), backed by `remaining_tickets`/`sold_out` fields on the Concert model. #222 (\"Let a fan see the full line-up for a concert\") is also shipped, with the lineup model, API, and `concert_detail.html` template all in place. #231 (\"Let a fan search concerts by artist name\") is likewise shipped, with a working htmx-powered search input on the dashboard wired to `artist_name`.\n\n## Priorities\n\nSince no genuine unstarted user story exists in the backlog, today's priority is not feature development but backlog hygiene: these duplicate/stale issues should be closed on GitHub with a reference to the PRs that already resolved them, so the dev team doesn't waste a cycle re-implementing shipped functionality. No user stories are being pulled into this sprint.\n\n## Risks\n\nThe main risk is wasted engineering effort if these issues are picked up at face value without verifying current code state — this has happened before (see prior cycles where already-shipped tickets were nearly re-scheduled). A secondary risk is that the backlog itself needs replenishing with real new user stories; until product/stakeholders add new scope, the team has no forward-looking work queued beyond bug fixes or tech debt.\n\n## Recommendation\n\nRecommend flagging to stakeholders that the backlog is empty of actionable new work and requesting new user stories be added, rather than assigning engineers to re-verify or pad out already-complete features."
}
```
