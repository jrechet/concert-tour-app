# Daily Plan — 2026-09-23

Confirmed: `remaining_tickets` and `sold_out` are real model properties, and the template already renders both the "Sold Out" badge and the "N tickets left" count. This matches an already-verified daily plan found at `docs/daily-plans/2026-09-23.md`. Every open backlog issue is already shipped, so no new sprint work exists today.

```json
{
  "selected": [],
  "daily_plan": "## Today's Goals\n\nAfter verifying the codebase directly, every issue currently open in the backlog is already fully implemented on `main`. Issues #259, #252, #246, and #198 all share the identical title \"Show the remaining ticket count on each concert card\" and are duplicates of previously-shipped work — `src/templates/_concert_card.html` (lines 12-16) already renders both the sold-out badge and the `{{ concert.remaining_tickets }} tickets left` count, backed by `remaining_tickets` and `sold_out` properties on the `Concert` model (`src/models/concert.py`). #265 (\"Show a sold out badge when no tickets remain\") is covered by the same markup — the `ticket-badge-sold-out` span already exists and is gated on `concert.sold_out`. #222 (\"Let a fan see the full line-up for a concert\") is also shipped, with the lineup model, API, and `concert_detail.html` template in place. #231 (\"Let a fan search concerts by artist name\") is likewise shipped, with a working search input wired to `artist_name`.\n\n## Priorities\n\nSince no genuine unstarted user story exists in the backlog, today's priority is backlog hygiene rather than feature development: these duplicate/stale issues should be closed on GitHub with references to the PRs that already resolved them (#204/#220/#221/#226/#227/#228/#243-245/#263, per prior verification), so the dev team doesn't waste a cycle re-implementing shipped functionality.\n\n## Risks\n\nThe main risk is wasted engineering effort if any of these issues are picked up at face value without checking current code state — this has repeatedly happened across prior cycles (e.g. #198, #254, #231, #239, #222 were all nearly rescheduled despite being done). A secondary risk is that the backlog needs replenishing with real new scope; until stakeholders add new user stories, the team has no forward-looking work queued.\n\n## Recommendation\n\nFlag to stakeholders that the backlog is empty of actionable new work and request new user stories, rather than assigning engineers to re-verify or pad out already-complete features."
}
```
