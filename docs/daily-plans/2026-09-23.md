# Daily Plan — 2026-09-23

I've verified enough — dashboard already has a city search box wired to the `city` API param, but nothing equivalent for `artist_name` even though the backend filter exists. That confirms #231 just needs the frontend piece.

```json
{
  "selected": [
    {
      "number": 273,
      "title": "Sort the tour dates chronologically with the next concert first",
      "reason": "Verified this is a genuine gap: TourResponse has no concerts list at all and no route renders a tour's dates, so there's nothing sorted yet. High business value (fans need to see upcoming dates in order) and it's foundational for any future per-tour itinerary view."
    },
    {
      "number": 231,
      "title": "Let a fan search concerts by artist name",
      "reason": "Backend already supports an artist_name filter on GET /api/v1/concerts and the dashboard route, but there is no fan-facing search box for it (only city has one, in concert-search.js). Low-risk, mostly frontend work that mirrors the existing city search pattern, so it can ship fast and complete the feature end-to-end."
    }
  ],
  "daily_plan": "Today's sprint focuses on two real gaps confirmed by direct code inspection rather than issue titles alone. #273 (chronological tour dates, next concert first) is the priority: there is currently no per-tour concert listing at all — TourResponse carries no concerts, and no route renders them — so this requires adding a concerts relationship/endpoint for a tour plus ordering by date_time with past dates excluded or de-prioritized so the 'next' concert leads. This is foundational and should land before any future itinerary/calendar work builds on top of it.\n\n#231 (artist-name search) is lower risk and should be quick: the API and dashboard route already accept an artist_name query parameter, so today's work is purely the fan-facing UI — a search input plus debounced fetch/render, following the same pattern already shipped for the city filter (src/static/js/concert-search.js). This closes out the search feature set with minimal backend risk.\n\nImportant housekeeping: #259, #252, #246, and #198 are duplicate copies of 'Show the remaining ticket count on each concert card,' and #265 ('sold out badge'), #266 ('city search box'), and #222 ('full lineup view') are all already fully implemented in the codebase (Concert.remaining_tickets/sold_out properties, the ticket-count/sold-out badges in _concert_card.html and concert-search.js, the city filter end-to-end, and the concert_detail.html lineup section backed by GET /concerts/{id}/lineup). None of these were selected for the sprint — re-implementing them would risk duplicate work or low-value PRs. Recommend closing all of these issues as already resolved/duplicate rather than assigning further engineering time.\n\nMain risk for #273 is scope creep into a full tour-detail page; keep it narrow to the sorted concert list itself. For #231, the main risk is duplicating logic between the city and artist search boxes — consider a small shared render/debounce helper if both boxes end up coexisting on the dashboard."
}
```
