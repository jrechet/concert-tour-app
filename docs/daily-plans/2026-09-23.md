# Daily Plan — 2026-09-23

```json
{
  "selected": [],
  "daily_plan": "Direct code inspection shows every open backlog issue is already implemented and tested on `main`, so nothing genuinely new is being pulled into today's sprint. #280 (upcoming-concerts JSON endpoint) landed via PR #284 (`GET /api/v1/concerts/upcoming`, with real filtering logic in `concerts.py` plus five passing integration tests, not just a test-only PR). #231 (artist-name search) also turned out to be done: `dashboard.html` has a live `#artist-search-input` wired via HTMX to `GET /api/v1/dashboard/concerts?artist_name=...`, covered end-to-end in `tests/test_concert_search.py` (partial/case-insensitive match, blank-clears-filter, empty state, and XSS-escaping). #285/#266 (city search box) are shipped as both a debounced live input and a populated dropdown (`concert-search.js`, `test_concert_city_filter.py`, `test_concert_city_search_ui.py`). #259/#252/#246/#198 (remaining ticket count) and #265 (sold-out badge) are all live in `Concert.remaining_tickets`/`sold_out` and rendered in both `_concert_card.html` and `concert-search.js`. #273 (chronological ordering) is implemented via the `is_past` ordering in `GET /api/v1/concerts/`. #222 (full line-up) has both `GET /api/v1/concerts/{id}/lineup` (ordered, paginated, 404-on-missing, tested in `test_concert_lineup_api.py`) and a rendered `/concerts/{id}` detail page.\n\nNo risks or dependency blockers today since no code is being touched. The only action item is backlog hygiene: recommend closing #285, #280, #273, #266, #265, #259, #252, #246, #231, #222, and #198 as already resolved/duplicate, since carrying them forward risks the team re-implementing or padding already-shipped work (a pattern we've already seen once with PR #257's test-only changeset). Tomorrow's priority should be sourcing genuinely new stories from the product backlog rather than re-grooming this list, since it currently has zero actionable items left."
}
```
