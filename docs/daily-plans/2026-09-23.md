# Daily Plan — 2026-09-23

```json
{
  "selected": [
    {"number": 198, "title": "Show the remaining ticket count on each concert card", "reason": "Backend already exposes remaining ticket count via the concert GET endpoints (#257, #263), so the frontend card display can now be built on solid ground. Duplicates #246, #252, #259 track the same request and should be closed in favor of this one to avoid redundant work."},
    {"number": 265, "title": "Show a \"sold out\" badge on a concert card when no tickets remain", "reason": "Directly depends on the remaining ticket count being available on the card (#198); sequencing it right after avoids rework and delivers a clear fan-facing signal that reduces frustration from clicking into sold-out shows."},
    {"number": 231, "title": "Let a fan search concerts by artist name", "reason": "High business value discovery feature independent of the ticket-count work, so it can proceed in parallel; artist-name search is a more common fan intent than city search (#266), which covers similar ground and can wait."}
  ],
  "daily_plan": "Today's focus is closing out the remaining-ticket-count feature end-to-end now that the backend groundwork from #257/#263 has landed. We'll implement #198 (display remaining ticket count on the concert card) as the canonical fix for what is otherwise a four-way duplicate (#198, #246, #252, #259) — once merged, the other three should be closed as duplicates to keep the backlog clean. Immediately after, we'll layer #265 (sold-out badge) on top, since it's a small conditional render on the same ticket-count field and delivers strong UX value by preventing fans from clicking into dead concerts.\n\nIn parallel, we'll pick up #231 (search concerts by artist name) as a discovery feature that doesn't depend on the ticket-count work. This gives the team two independent workstreams so a blocker on one doesn't stall the whole sprint.\n\nKey risks: per team memory, past PRs (#192, #257) have claimed functionality without actually implementing it (e.g. test-only diffs, binary artifacts) — we'll verify each PR's diff actually contains model/schema/endpoint/frontend changes matching its description before merging. We'll also watch for pagination on any collection endpoints touched and avoid unscoped test-infrastructure changes. #266 (search by city) and #222 (full line-up view) are deferred to a future sprint since they overlap conceptually with #231 or add scope beyond today's capacity."
}
```
