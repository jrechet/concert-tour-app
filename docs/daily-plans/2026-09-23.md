# Daily Plan — 2026-09-23

Confirmed: #273 is fully implemented (per-tour chronological dates), and #231's backend filter exists but there's still no artist-search UI. There's no dedicated "upcoming only" endpoint (#280) — the current `/api/v1/concerts/` returns everything, just ordered upcoming-first, with no way to filter out past dates.

```json
{
  "selected": [
    {
      "number": 280,
      "title": "Add a JSON endpoint listing the upcoming concerts",
      "reason": "Verified gap: GET /api/v1/concerts/ orders upcoming concerts first but still returns past ones too, and GET /api/v1/tours/{id}/dates has the same behavior. There's no way for a client to fetch only future concerts. High value for any public-facing 'what's coming up' view and straightforward to add given the existing is_past ordering logic already in concerts.py/tours.py."
    },
    {
      "number": 231,
      "title": "Let a fan search concerts by artist name",
      "reason": "Carried over from today's earlier planning pass: the artist_name backend filter exists on GET /api/v1/concerts and the dashboard route, but src/static/js/concert-search.js still only wires up the city input/dropdown \u2014 no artist search box was added. Low risk, mirrors the shipped city-search pattern, and closes out the search feature end-to-end."
    }
  ],
  "daily_plan": "Two genuine gaps remain after direct code inspection, and everything else in the open backlog is already implemented and should be closed rather than re-worked. #259, #252, #246, and #198 are duplicate copies of 'Show the remaining ticket count on each concert card' \u2014 done (Concert.remaining_tickets, ticket-count badge in _concert_card.html and concert-search.js). #265 (sold-out badge), #266 (city search, now with both a live input and a populated dropdown per commit de0f37f), #222 (full concert lineup via GET /concerts/{id}/lineup), and #273 (chronological tour dates, both on the dashboard list and now per-tour via GET /tours/{id}/dates, per commits 3a3fbe9/7a0fe75) are all confirmed shipped and tested.\n\nToday's priority is #280: adding a filtered 'upcoming only' JSON endpoint. The is_past ordering logic already exists in both concerts.py and tours.py, so this is mostly a matter of exposing an upcoming boolean/query param (or a dedicated route) that filters rather than just orders, plus tests confirming past and today's-date-boundary concerts are excluded/included correctly. Keep it scoped to filtering \u2014 don't fold in new response fields or restructure the existing ordering behavior other clients depend on.\n\n#231 is the secondary priority: it's pure frontend work mirroring the existing city search box in concert-search.js, wiring a new artist-name input to the already-working artist_name query param. Main risk is code duplication between the two search boxes (debounce/render logic) \u2014 worth a small shared helper if both end up on the same page, but don't over-engineer beyond that.\n\nMain risk overall is scope creep: for #280, resist the urge to build a broader 'concerts feed' with extra filtering/sorting options beyond upcoming-only; for #231, resist merging it into a single generic 'search' component before there's a second confirmed need. Recommend closing #259, #252, #246, #198, #265, #266, #222 as already resolved/duplicate to keep the backlog honest."
}
```
