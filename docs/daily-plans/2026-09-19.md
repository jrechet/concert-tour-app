# Daily Plan — 2026-09-19

```json
{
  "selected": [
    {"number": 215, "title": "Warn a fan when a concert is almost sold out", "reason": "Backend (is_almost_sold_out property/API) already shipped on 2026-09-13; only the badge markup in the concert card template is missing. Smallest remaining gap, high conversion/urgency value, ready to close today."},
    {"number": 205, "title": "Let a fan filter the concert list by city", "reason": "Backend city filter API is already shipped and tested; only a UI picker in the dashboard template remains. Low-effort, unlocks value already invested in the backend."},
    {"number": 239, "title": "Marquer une date de tournee comme annulee", "reason": "No implementation exists yet (no model field, API, or UI). It's the only remaining backlog item requiring net-new full-stack work, and fans need accurate cancellation status — start the model/migration layer today per our foundational-layers-first practice."}
  ],
  "daily_plan": "Today's sprint closes out two nearly-finished features and kicks off one net-new one. #215 and #205 both already have working, tested backend support (is_almost_sold_out property and the city query param respectively) — the only work left is template/UI: add a sold-out-soon badge to the concert card partial, and a city filter control above the concert list. These are low-risk, same-day wins that unlock value we've already paid for on the backend.\n\n#239 (cancel a tour date) is the priority for new build work: verification confirmed zero existing support — no model field, no API, no template hooks. Per our standing rule to build foundational layers before UI, today's work on this ticket should stop at the model/migration + API layer; do not start on cancellation UI until that backend groundwork lands and is reviewed, to avoid the kind of incomplete-diff issue we hit with PR #192.\n\nRisks: #239 is the one item that could slip — if the model/migration work reveals unexpected coupling with the Tour.status field, we may need to split it into a follow-up ticket rather than rush a UI. #215 and #205 carry low risk since they're template-only changes against already-tested APIs, but we should double check existing tests aren't broken by the new markup.\n\nNote: verification today found #231 (artist search) is already fully shipped, including the UI, despite still being open on GitHub — recommend closing it out of the backlog so it doesn't get re-selected in a future sprint."
}
```
