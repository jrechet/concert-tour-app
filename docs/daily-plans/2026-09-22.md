# Daily Plan — 2026-09-22

I verified the current code state directly rather than relying solely on memory:

- **#198, #246** ("Show remaining ticket count on each concert card" — duplicate titles): already implemented — `_concert_card.html` renders `concert.remaining_tickets` and a sold-out badge.
- **#231** (artist search): already implemented — dashboard has a live search input wired to the API.
- **#222** (full line-up): already shipped per memory, unrelated to current gaps.
- **#215** (almost-sold-out warning): backend `is_almost_sold_out` exists on the model/schema, but no badge markup in `_concert_card.html` — genuinely open.
- **#205** (city filter): backend query param exists in `concerts.py`, but no city picker UI anywhere in the templates — genuinely open.

Only two issues represent real, unstarted work. I'm selecting both for today's sprint.

```json
{
  "selected": [
    {"number": 205, "title": "Let a fan filter the concert list by city", "reason": "Backend filter (case-insensitive city match) already shipped in src/routers/concerts.py, but no picker exists in the dashboard templates — pure UI work, low risk, unblocks a feature that's already half-built."},
    {"number": 215, "title": "Warn a fan when a concert is almost sold out", "reason": "Model property is_almost_sold_out and API exposure already landed; only the badge markup in _concert_card.html is missing. Small, well-scoped UI addition with clear precedent from the existing sold-out badge."}
  ],
  "daily_plan": "Both stories selected today are UI-only follow-ups to backend work that already shipped — this keeps risk low and lets us close out two issues without touching models, routers, or migrations. Verified directly against the current templates and routers before selecting, since several open backlog issues (#198, #246, #231, #222) turned out to already be fully implemented in the current code despite still being listed open; those should be closed rather than re-worked.\n\nPriority for #205 (city filter UI): add a select/input to dashboard.html wired via htmx to the existing `city` query param on GET /api/v1/dashboard/concerts (or /api/v1/concerts, whichever the dashboard partial uses), following the same hx-include pattern already used for the artist-search input and cancelled-checkbox so all three filters compose together. Priority for #215 (almost-sold-out badge): add a conditional badge block in _concert_card.html keyed off `concert.is_almost_sold_out`, mirroring the existing `sold_out` badge styling but visually distinct (e.g. warning color vs. sold-out red).\n\nMain risk is filter interaction: the dashboard already combines artist search + cancelled-hide via hx-include, so the city filter must be added to that same hx-include chain on all three trigger elements or filters will silently overwrite each other instead of composing. For the badge, watch the precedence with the existing sold_out badge in the template — is_almost_sold_out should only render when NOT already sold_out (the model likely already guards this, but the template's conditional order should be checked explicitly).\n\nNo new backend, migrations, or test-infrastructure changes are needed for either story, so today's scope should stay template/route-wiring only. Flagging #198/#246/#231/#222 to the backlog owner for closure as already-shipped duplicates so they stop resurfacing in future sprint planning."
}
```
